# Never publish the recommended textbooks — they are copyrighted.
# Students obtain them from eLearning, per the course outline.
*.pdf

# Local component gallery, not part of the student-facing site.
preview.html

# Dev-only test dependencies. The site itself has none; dev/check-site.mjs
# is committed but its jsdom install is not.
dev/node_modules/
dev/package*.json

# Local tool settings. Claude Code writes granted permissions here, and those
# records name the private decks folder by absolute path — which this public
# repository must not carry. check-decks.mjs's privacy guard reads the served
# pages only, so it would not catch a leak from here.
.claude/

.DS_Store
